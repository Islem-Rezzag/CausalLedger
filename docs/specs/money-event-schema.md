# MoneyEvent Schema

Placeholder for the canonical MoneyEvent schema. No MoneyEvent logic is implemented yet.

## Domain dependency

Future MoneyEvent schema work may use the M01.01 payment lifecycle vocabulary in `docs/domain/payment-lifecycle.md`. This note is a dependency pointer only; it does not define schema fields or runtime behavior.
