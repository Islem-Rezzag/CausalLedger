# MoneyEvent Schema

Placeholder for the canonical MoneyEvent schema. No MoneyEvent logic is implemented yet.

## Domain dependency

Future MoneyEvent schema work may use the M01.01 payment lifecycle vocabulary in `docs/domain/payment-lifecycle.md`, the M01.03 settlement vocabulary in `docs/domain/settlement-vocabulary.md`, the M01.04 reconciliation vocabulary in `docs/domain/reconciliation-vocabulary.md`, and incident boundary vocabulary in `docs/domain/incident-vocabulary.md`. This note is a dependency pointer only; it does not define schema fields or runtime behavior.
