# Incident Schema

Placeholder for future incident schema. No incident engine is implemented yet.

## Domain dependency

Future incident work may use the M01.01 payment lifecycle vocabulary in `docs/domain/payment-lifecycle.md` to describe lifecycle exceptions, the M01.02 ledger vocabulary in `docs/domain/ledger-vocabulary.md` to describe ledger exceptions, the M01.03 settlement vocabulary in `docs/domain/settlement-vocabulary.md` to describe settlement exceptions, the M01.04 reconciliation vocabulary in `docs/domain/reconciliation-vocabulary.md` to describe reconciliation exceptions, the M01.05 incident vocabulary in `docs/domain/incident-vocabulary.md` to describe incident concepts, statuses, severity, types, paths, evidence examples, questions, and failure patterns, the M01.08 human review states in `docs/domain/human-review-states.md` to describe review, closure, escalation, and approval boundaries, and the M01.09 out-of-scope boundaries in `docs/domain/out-of-scope-domains.md` to avoid unsupported domain claims. This note is a dependency pointer only; it does not define incident states, schemas, or runtime behavior.
