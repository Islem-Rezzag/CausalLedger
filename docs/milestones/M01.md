# M01 Domain model and scope freeze

## Milestone goal

Freeze CausalLedger domain language, scope, and non-goals.

## Why it matters

Money-movement systems need precise language before schemas, invariants, or tools are built.

## Detailed submilestones

| ID | Name | Status | Notes |
| --- | --- | --- | --- |
| M01.01 | Define payment lifecycle | Completed and merged | Domain documentation only. Builder PR #11 was squash-merged before required QA; post-merge QA recovery passed on branch `m01-01-qa-recovery-define-payment-lifecycle`. Do not start M01.02 until the QA recovery PR merges. |
| M01.02 | Define ledger vocabulary | Not started | Future slice; do not start without an active plan. |
| M01.03 | Define settlement vocabulary | Not started | Future slice; do not start without an active plan. |
| M01.04 | Define reconciliation vocabulary | Not started | Future slice; do not start without an active plan. |
| M01.05 | Define incident vocabulary | Not started | Future slice; do not start without an active plan. |
| M01.06 | Define safe and unsafe repairs | Not started | Future slice; do not start without an active plan. |
| M01.07 | Define evidence receipt model | Not started | Future slice; do not start without an active plan. |
| M01.08 | Define human review states | Not started | Future slice; do not start without an active plan. |
| M01.09 | Define out-of-scope domains | Not started | Future slice; do not start without an active plan. |
| M01.10 | Write DOMAIN_MODEL.md | Not started | Future slice; do not start without an active plan. |
| M01.11 | Write RELIABILITY.md | Not started | Future slice; do not start without an active plan. |
| M01.12 | Write THREAT_MODEL.md | Not started | Future slice; do not start without an active plan. |
| M01.13 | QA domain consistency | Not started | Future slice; do not start without an active plan. |

## Acceptance criteria

- Domain model and scope are approved.
- Safety boundaries remain consistent with `AGENTS.md` and active docs.
- Scope is limited to this milestone and its active plan.
- Validation evidence is recorded in the active plan and status docs.
- Handoff notes identify what changed, what did not change, and the next safe thread.

## Validation expectations

Review domain docs for consistency, non-goals, and safety-boundary alignment.

M01.01 builder validation passed on 2026-05-11 after one scoped tracking wording fix:

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 18 tests.
- `git diff --check` passed.
- `make bootstrap-check` was not run because `make` is unavailable in the current Windows shell.

M01.01 post-merge QA recovery passed on 2026-05-11 after auditing the merged builder work at commit `1175789` (`docs: define M01.01 payment lifecycle (#11)`):

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 18 tests.
- `git diff --check` passed.
- `make bootstrap-check` was not run because `make` is unavailable in the current Windows shell.

## Status

Active. M01 planning is complete and merged at git commit `2cfd75a` (`docs: plan M01 domain model and scope freeze (#10)`). The active M01 plan is `plans/active/CLP-0002-m01-domain-model-and-scope-freeze.md`. M01.01 builder PR #11 was squash-merged before required QA; this milestone records the post-merge QA recovery.

M01.01 is `Completed and merged` after post-merge QA recovery. M01.02 through M01.13 remain `Not started`.

## Handoff notes

- M00 is complete and tagged as `v0.1.0`.
- This milestone planning thread may update docs, specs, tracking, versioning, and validation only.
- M01.01 may define payment lifecycle vocabulary as domain documentation only.
- Treat submilestones here as planned scope, not implemented behavior.
- Do not start M01.02 until the M01.01 QA recovery PR merges.
