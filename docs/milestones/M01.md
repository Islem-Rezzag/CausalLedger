# M01 Domain model and scope freeze

## Milestone goal

Freeze CausalLedger domain language, scope, and non-goals.

## Why it matters

Money-movement systems need precise language before schemas, invariants, or tools are built.

## Detailed submilestones

| ID | Name | Status | Notes |
| --- | --- | --- | --- |
| M01.01 | Define payment lifecycle | Completed and merged | Domain documentation only. Builder PR #11 was squash-merged before required QA; post-merge QA recovery passed on branch `m01-01-qa-recovery-define-payment-lifecycle` and merged before M01.02 began. |
| M01.02 | Define ledger vocabulary | Completed and merged | Domain documentation only; merged at commit `fd1e259` (`docs: define M01.02 ledger vocabulary (#13)`). |
| M01.03 | Define settlement vocabulary | Completed and merged | Domain documentation only; merged at commit `e54a917` (`docs: define M01.03 settlement vocabulary (#14)`). |
| M01.04 | Define reconciliation vocabulary | Completed and merged | Domain documentation only; merged at commit `5dfe928` (`docs: define M01.04 reconciliation vocabulary (#15)`). |
| M01.05 | Define incident vocabulary | Completed and merged | Builder PR #16 was squash-merged before required QA at commit `5c3943b`; post-merge QA recovery PR #18 merged at commit `3bdedeb`. Domain vocabulary documentation plus scoped ablation roadmap/evaluation planning only; no product implementation or runtime behavior. |
| M01.06 | Define safe and unsafe repairs | Completed and merged | Domain documentation only; PR #21 merged at git commit `7adc96d` (`docs: define M01.06 safe and unsafe repairs (#21)`). Defines repair proposal vocabulary, safe-to-propose repair categories, unsafe and forbidden autonomous repair boundaries, evidence requirements, deterministic validation expectations, replay-before-apply, rollback, idempotency, human approval, escalation, and moat rationale. No product implementation or runtime repair behavior. |
| M01.07 | Define evidence receipt model | Completed and merged | Domain documentation only; PR #23 merged at git commit `a88b5ff` (`docs: define M01.07 evidence receipt model (#23)`), with local `main`/`origin/main` confirmed at `0313f4e` (`docs: define M01.07 evidence receipt model`) before post-merge finalization. Defines evidence receipt vocabulary, provenance, chain of custody, timestamps, freshness, retention, redaction, confidentiality, uncertainty, confidence, limitations, conflicts, coverage, gaps, evidence bundles, receipt statuses, rejection reasons, append-only handling, immutable raw evidence boundaries, derived evidence boundaries, audit trails, safety boundaries, and moat rationale. No product implementation or runtime evidence behavior. |
| M01.08 | Define human review states | Completed and merged | Domain documentation only; PR #26 merged at git commit `1fde07a` (`docs: define M01.08 human review states (#26)`). Defines human review vocabulary, review actors, statuses, decision boundaries, repair-review states, approval and rejection boundaries, AI review boundaries, evidence expectations, correctness questions, failure patterns, and audit constraints. No product implementation or runtime human-review behavior. |
| M01.09 | Define out-of-scope domains | Completed and merged | Domain documentation only; PR #27 merged at git commit `1b40773` (`docs: define M01.09 out-of-scope domains (#27)`). Defines hard out-of-scope domains, adjacent-but-not-core domains, forbidden product claims, LLM forbidden actions, future-extension rules, interview and product positioning boundaries, and examples. No product implementation or runtime behavior. |
| M01.10 | Write DOMAIN_MODEL.md | Completed and merged | Builder PR #28 was squash-merged before required QA at commit `dc6800b`; QA recovery PR #29 merged at git commit `a878d55` (`test: QA recovery M01.10 domain model summary (#29)`). Documentation-only canonical M01 domain model summary; no product implementation or runtime behavior. |
| M01.11 | Write RELIABILITY.md | Completed and merged | Documentation-only reliability model in `docs/RELIABILITY.md`; PR #30 merged at git commit `a424924` (`docs: write M01.11 reliability model (#30)`). Defines deterministic-first reliability expectations, financial truth model, evidence reliability, ledger reliability, settlement/reconciliation reliability, incident reliability, replay reliability, repair reliability, human review reliability, agentic AI reliability, model/cost reliability, auditability, ablations, failure modes, metrics, future dependencies, and guardrails. No runtime reliability mechanisms or product behavior. |
| M01.12 | Write THREAT_MODEL.md | Completed and merged | Documentation-only threat model in `docs/THREAT_MODEL.md`; PR #31 merged, and duplicate PR merges #32 and #33 from the same M01.12 branch are recorded as a process deviation. Defines protected assets, trust boundaries, actors/adversaries, evidence threats, ledger and financial truth threats, settlement/reconciliation threats, incident/replay threats, repair/human review threats, agentic AI threats, prompt injection threats, tool/permission threats, privacy, secrets, supply chain, model routing and cost threats, ablation threats, operational/governance threats, mitigation matrix, future dependencies, and guardrails. No revert is being done in this finalization thread; future submilestones should use one PR per branch and delete the branch after merge. No runtime security controls or product behavior. |
| M01.13 | QA domain consistency | Not started | Future slice; do not start without an active plan. |

## Acceptance criteria

- Domain model and scope are approved.
- Safety boundaries remain consistent with `AGENTS.md` and active docs.
- Scope is limited to this milestone and its active plan.
- Validation evidence is recorded in the active plan and status docs.
- Handoff notes identify what changed, what did not change, and the next safe thread.

## Validation expectations

Review domain docs for consistency, non-goals, and safety-boundary alignment.

M01.01 builder validation passed on 2026-05-11 after one scoped tracking wording fix:

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 18 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.

M01.01 post-merge QA recovery passed on 2026-05-11 after auditing the merged builder work at commit `1175789` (`docs: define M01.01 payment lifecycle (#11)`):

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 18 tests.
- `git diff --check` passed.
- `make bootstrap-check` was not run because `make` is unavailable in the current Windows shell.

M01.02 builder validation passed on 2026-05-15 after one scoped documentation wording fix:

- Initial `python scripts/validate-control-plane.py` failed because the ledger vocabulary doc defined some terms only in title-cased table cells while validation expected lower-case phrases.
- `python scripts/validate-control-plane.py` passed after adding scoped lower-case vocabulary-list sentences.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 19 tests.
- `git diff --check` passed.
- `make bootstrap-check` was not run because `make` is unavailable in the current Windows shell.

M01.02 QA validation passed on 2026-05-15 after documentation-only review:

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 19 tests.
- `git diff --check` passed.
- `make bootstrap-check` was not run because `make` is unavailable in the current Windows shell.

M01.03 builder validation passed on 2026-05-15 after documentation-only settlement vocabulary work:

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 20 tests.
- `git diff --check` passed.
- `make bootstrap-check` was not run because `make` is unavailable in the current Windows shell.

M01.03 QA validation passed on 2026-05-16 after documentation-only review:

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 20 tests.
- `git diff --check` passed.
- `make bootstrap-check` was not run because `make` is unavailable in the current Windows shell.

M01.04 builder validation passed on 2026-05-16 after documentation-only reconciliation vocabulary work:

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 21 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.

M01.04 QA validation passed on 2026-05-16 after documentation-only reconciliation vocabulary review:

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 21 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.

M01.05 builder validation passed on 2026-05-16 after documentation-only incident vocabulary and ablation planning work and one scoped case-sensitive vocabulary wording fix:

- Initial `python scripts/validate-control-plane.py` failed because two incident severity dimensions appeared only with initial capitals while validation expected lower-case phrases.
- The scoped wording fix lower-cased the incident severity dimension bullets.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 23 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.

M01.05 post-merge QA recovery passed on 2026-05-16 after auditing the already-merged builder work at commit `5c3943b` (`docs: define M01.05 incident vocabulary and ablation strategy (#16)`):

- The QA recovery found no product implementation, runtime logic, ablation runner, benchmark code, runtime toggles, or future milestone start.
- QA applied scoped documentation/tracking clarifications only.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 23 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.

M01.05 post-merge finalization passed on 2026-05-17 after confirming local `main`, `origin/main`, and the current branch at commit `3bdedeb` (`test: QA recovery M01.05 incident vocabulary and ablation strategy (#18)`):

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py -q` passed with 23 tests.
- `git diff --check` passed.

M01.06 builder validation passed on 2026-05-17 after documentation-only safe and unsafe repair vocabulary work:

- Initial `python scripts/validate-control-plane.py` failed because `docs/status/NEXT_RECOMMENDED_THREAD.md` blocked M01.07 and M02 in combined wording while validation expected the exact `Do not start M02` phrase; the scoped wording was fixed and validation was rerun successfully.
- `python scripts/validate-control-plane.py` passed after the scoped wording fix.
- `python -m pytest tests/test_control_plane_bootstrap.py -q` passed with 24 tests.
- `git diff --check` passed.

M01.06 QA validation passed on 2026-05-18 after strict documentation-only repair-boundary review:

- QA confirmed safe repair means safe to propose for human review, not safe for autonomous application.
- QA confirmed unsafe repair categories, forbidden autonomous repair categories, evidence requirements, deterministic validation, replay-before-apply, idempotency, rollback, blast radius, audit trail, rejection reasons, and moat rationale remain vocabulary only.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py -q` passed with 24 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- At QA time, PR #21 body still contained default template placeholders; `gh` was unavailable, so this was recorded as a pre-merge accuracy note.

M01.06 post-merge finalization passed on 2026-05-18 after confirming PR #21 merged into `main` at git commit `7adc96d` (`docs: define M01.06 safe and unsafe repairs (#21)`):

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py -q` passed with 24 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.

M01.07 builder validation passed on 2026-05-18 after documentation-only evidence receipt model work:

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py -q` passed with 25 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- Product implementation has not started; no evidence ingestion runtime, evidence storage layer, database, schema, API, parser, connector, MoneyEvent runtime, ledger runtime, incident runtime, repair runtime, agent runtime, GitHub Action, CI workflow, deployment, auth/authz implementation, structured logging implementation, or product behavior was added.

M01.07 QA validation passed on 2026-05-18 after strict documentation-only evidence receipt review:

- QA confirmed the evidence receipt model covers source identity, provenance, chain of custody, timestamps, freshness, retention, redaction, confidentiality, uncertainty, confidence, limitations, conflicts, coverage, gaps, bundles, receipt statuses, rejection reasons, append-only handling, immutable raw evidence boundaries, derived evidence boundaries, audit trails, safety boundaries, and moat rationale.
- QA confirmed evidence receipts support investigation and repair proposals but do not become financial truth, approve repairs, mutate money, mutate financial truth, delete evidence, modify raw evidence, post ledger entries, override invariants, or replace source evidence with LLM output.
- QA found a UTF-8 BOM in `docs/milestones/SUBMILESTONE_REGISTRY.md`; the same BOM exists on `origin/main`, so it is benign and pre-existing. No risky bidirectional, isolate, or zero-width characters were introduced by M01.07.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py -q` passed with 25 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- Product implementation has not started; no evidence ingestion runtime, evidence storage layer, database, schema, API, parser, connector, MoneyEvent runtime, ledger runtime, incident runtime, repair runtime, agent runtime, GitHub Action, CI workflow, deployment, auth/authz implementation, structured logging implementation, or product behavior was added.

M01.07 post-merge finalization passed on 2026-05-18 after confirming PR #23 merged into `main` at git commit `a88b5ff` (`docs: define M01.07 evidence receipt model (#23)`) and local `main`/`origin/main` were at git commit `0313f4e` (`docs: define M01.07 evidence receipt model`):

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py -q` passed with 25 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- Product implementation has not started; no evidence ingestion runtime, evidence storage layer, database, schema, API, parser, connector, MoneyEvent runtime, ledger runtime, incident runtime, repair runtime, agent runtime, GitHub Action, CI workflow, deployment, auth/authz implementation, structured logging implementation, or product behavior was added.

M01.08 builder validation passed on 2026-05-21 after documentation-only human review state vocabulary work:

- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 26 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- Product implementation has not started; no human-review runtime, review queue, state machine, approval engine, API, database, UI, repair execution, agent runtime, GitHub Action, CI workflow, or product behavior was added.

M01.08 QA passed on 2026-05-21 after reviewing documentation-only human review state vocabulary, tracking, validation coverage, and forbidden-scope boundaries:

- No human-review vocabulary content defects were found.
- QA changes were limited to tracking/status/control-plane validation updates.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 26 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- M01.08 was `QA passed, awaiting merge`; it was not `Completed and merged` until PR #26 merged and M01.09 builder recorded post-merge finalization.
- M01.09 through M01.13 remained `Not started`.
- Product implementation has not started; no human-review runtime, review queue, state machine, approval engine, API, database, UI, repair execution, agent runtime, GitHub Action, CI workflow, or product behavior was added.

M01.08 post-merge finalization was recorded on 2026-05-21 at the beginning of M01.09 builder work after confirming PR #26 merged at git commit `1fde07a` (`docs: define M01.08 human review states (#26)`).

M01.09 builder validation passed on 2026-05-21 after documentation-only out-of-scope domain boundary work:

- `docs/domain/out-of-scope-domains.md` was added as domain boundary documentation only.
- Initial `python scripts/validate-control-plane.py` failed because the new M01.09 validation strings expected lower-case phrases while the domain doc used capitalized Markdown bullets and table cells; the scoped validation and bootstrap-test strings were aligned to the documentation and validation was rerun successfully.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 27 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- M01.09 is `Builder complete, awaiting QA`; it is not QA passed or completed.
- M01.10 through M01.13 remain `Not started`.
- Product implementation has not started; no MoneyEvent runtime, ledger runtime, settlement runtime, reconciliation runtime, incident runtime, invariant engine, causal graph runtime, replay runtime, agent runtime, repair planner runtime, human-review runtime, UI, external connector, database schema, API route, GitHub Action, CI workflow, autonomous repair execution, autonomous money movement, legal/tax/AML/KYC/fraud/credit/investment advice, or product behavior was added.

M01.09 QA passed on 2026-05-21 after reviewing documentation-only out-of-scope domain boundaries, tracking, validation coverage, and forbidden-scope boundaries:

- No out-of-scope domain content defects were found.
- QA changes were limited to tracking/status/control-plane validation updates.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 27 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- M01.09 is `QA passed, awaiting merge`; it is not `Completed and merged` until PR merge and post-merge finalization.
- M01.10 through M01.13 remain `Not started`.
- Product implementation has not started; no MoneyEvent runtime, ledger runtime, settlement runtime, reconciliation runtime, incident runtime, invariant engine, causal graph runtime, replay runtime, agent runtime, repair planner runtime, human-review runtime, UI, external connector, database schema, API route, GitHub Action, CI workflow, autonomous repair execution, autonomous money movement, legal/tax/AML/KYC/fraud/credit/investment advice, or product behavior was added.

## Status

Active. M01 planning is complete and merged at git commit `2cfd75a` (`docs: plan M01 domain model and scope freeze (#10)`). The active M01 plan is `plans/active/CLP-0002-m01-domain-model-and-scope-freeze.md`. M01.01 builder PR #11 was squash-merged before required QA; this milestone records the completed post-merge QA recovery.

M01.01 is `Completed and merged` after post-merge QA recovery. M01.02 is `Completed and merged`. M01.03 is `Completed and merged` at git commit `e54a917`. M01.04 is `Completed and merged` at git commit `5dfe928`. M01.05 is `Completed and merged` after QA recovery PR #18 merged at git commit `3bdedeb`. M01.06 is `Completed and merged` after PR #21 merged at git commit `7adc96d`. M01.07 is `Completed and merged` after PR #23 merged at git commit `a88b5ff`. M01.08 is `Completed and merged` after PR #26 merged at git commit `1fde07a`. M01.09 is `Completed and merged` after PR #27 merged at git commit `1b40773`. M01.10 is `Completed and merged` after QA recovery PR #29 merged at git commit `a878d55`. M01.11 is `Completed and merged` after PR #30 merged at git commit `a424924`. M01.12 is `Completed and merged` after PR #31 merged, with duplicate PR merges #32 and #33 recorded as a process deviation. M01.13 remains `Not started`.

## Handoff notes

- M00 is complete and tagged as `v0.1.0`.
- This milestone planning thread may update docs, specs, tracking, versioning, and validation only.
- M01.01 defined payment lifecycle vocabulary as domain documentation only.
- M01.02 defined ledger vocabulary as domain documentation only and is completed and merged.
- M01.03 defined settlement vocabulary as domain documentation only and is completed and merged.
- M01.04 defined reconciliation vocabulary as domain documentation only and is completed and merged.
- M01.05 defined incident vocabulary and scoped ablation roadmap/evaluation planning as documentation only. The builder PR merged before QA, and post-merge QA recovery PR #18 merged at commit `3bdedeb`.
- M01.06 defined safe and unsafe repair vocabulary as domain documentation only and is completed and merged after PR #21 merged at git commit `7adc96d`.
- M01.07 defined evidence receipt model vocabulary as domain documentation only and is completed and merged after PR #23 merged at git commit `a88b5ff`.
- M01.08 defined human review states as domain documentation only and is completed and merged after PR #26 merged at git commit `1fde07a`.
- M01.09 defined out-of-scope domains as domain documentation only and is completed and merged after PR #27 merged at git commit `1b40773`.
- M01.10 rewrote `docs/DOMAIN_MODEL.md` as the canonical M01 domain model summary; this remains documentation only and is completed and merged after QA recovery PR #29 merged at git commit `a878d55`.
- M01.11 rewrote `docs/RELIABILITY.md` as the canonical CausalLedger reliability model; this remains documentation only and is completed and merged after PR #30 merged at git commit `a424924`.
- M01.12 rewrote `docs/THREAT_MODEL.md` as the canonical CausalLedger threat model; this remains documentation only and is completed and merged after PR #31 merged. Duplicate PR merges #32 and #33 from the same branch are recorded as a process deviation; no revert is being done.
- Treat submilestones here as planned scope, not implemented behavior.
- Next recommended thread is `M01.13 Builder - QA Domain Consistency` after this finalization PR merges and local main is updated.

M01.10 builder validation passed on 2026-05-21 after documentation-only DOMAIN_MODEL.md summary work:

- Initial `python scripts/validate-control-plane.py` failed because the M01.10 registry row used lower-case `no product implementation or runtime behavior` while validation expected the exact capitalized marker; the scoped registry wording was fixed and validation was rerun successfully.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 27 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- M01.10 is `Builder complete, awaiting QA`; it is not QA passed or completed.
- M01.11 through M01.13 remain `Not started`.
- Product implementation has not started; no MoneyEvent runtime, ledger runtime, settlement runtime, reconciliation runtime, incident runtime, invariant engine, causal graph runtime, replay runtime, agent runtime, repair planner runtime, human-review runtime, UI, external connector, database schema, API route, GitHub Action, CI workflow, or product behavior was added.

M01.10 post-merge QA recovery passed on 2026-05-21 after auditing the already-merged builder work at commit `dc6800b` (`docs: write M01.10 domain model summary (#28)`):

- Builder PR #28 was squash-merged before the required QA thread.
- QA recovery ran on branch `m01-10-qa-recovery-domain-model`.
- QA confirmed `docs/DOMAIN_MODEL.md` is a coherent canonical M01 domain model summary, not just an index.
- QA found no DOMAIN_MODEL.md content defects and no product implementation.
- QA changes were limited to tracking/status/control-plane validation updates.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 27 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- At this point M01.10 was `QA recovery passed, awaiting recovery PR merge`; it was later finalized as `Completed and merged` after QA recovery PR #29 merged.
- M01.11 through M01.13 remain `Not started`.
- Product implementation has not started; no MoneyEvent runtime, ledger runtime, settlement runtime, reconciliation runtime, incident runtime, invariant engine, causal graph runtime, replay runtime, agent runtime, repair planner runtime, human-review runtime, UI, external connector, database schema, API route, GitHub Action, CI workflow, or product behavior was added.

M01.10 post-merge finalization was recorded on 2026-05-21 at the beginning of M01.11 builder work after confirming QA recovery PR #29 merged at git commit `a878d55` (`test: QA recovery M01.10 domain model summary (#29)`).

M01.11 builder validation passed on 2026-05-21 after documentation-only RELIABILITY.md model work:

- `docs/RELIABILITY.md` was rewritten as the canonical CausalLedger reliability model.
- README, docs index, domain model, active plan, roadmap, milestone doc, registry, status docs, capability matrix, tech debt, risk register, validation script, and bootstrap tests were updated for M01.11.
- Initial `python -m pytest tests/test_control_plane_bootstrap.py` failed because the new reliability-model test treated the required negative statement `LLM output is evidence` as a forbidden positive claim; the scoped test assertion was narrowed and validation was rerun successfully.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- M01.11 is `Builder complete, awaiting QA`; it is not QA passed or completed.
- M01.12 and M01.13 remain `Not started`.
- Product implementation has not started; no runtime reliability mechanism, MoneyEvent runtime, ledger runtime, settlement runtime, reconciliation runtime, incident runtime, invariant engine, causal graph runtime, replay runtime, agent runtime, repair planner runtime, human-review runtime, UI, external connector, database schema, API route, GitHub Action, CI workflow, or product behavior was added.

M01.11 QA passed on 2026-05-21 after reviewing documentation-only reliability model content, tracking/status files, validation coverage, and forbidden-scope boundaries:

- QA confirmed `docs/RELIABILITY.md` is a coherent CausalLedger reliability model covering status, purpose, thesis, scope, financial truth, deterministic-first reliability, evidence, ledger/accounting, settlement/reconciliation, incident, replay, repair, human review, agentic AI, model routing and cost, observability, evaluation and ablation, security dependency, failure modes, metrics, future dependencies, remaining M01 work, and guardrails.
- QA found no RELIABILITY.md content defects and no product implementation.
- QA changes were limited to tracking/status/control-plane validation updates.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 28 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- M01.11 is `QA passed, awaiting merge`; it is not `Completed and merged` until PR merge and post-merge finalization.
- M01.12 and M01.13 remain `Not started`.
- Product implementation has not started; no runtime reliability mechanism, MoneyEvent runtime, ledger runtime, settlement runtime, reconciliation runtime, incident runtime, invariant engine, causal graph runtime, replay runtime, agent runtime, repair planner runtime, human-review runtime, UI, external connector, database schema, API route, GitHub Action, CI workflow, or product behavior was added.

M01.11 post-merge finalization was recorded on 2026-05-21 at the beginning of M01.12 builder work after confirming PR #30 merged at git commit `a424924` (`docs: write M01.11 reliability model (#30)`).

M01.12 builder started on 2026-05-21 for documentation-only THREAT_MODEL.md work:

- Branch guard passed on `m01-12-write-threat-model`.
- Starting worktree was clean.
- Latest commit was `a424924` (`docs: write M01.11 reliability model (#30)`).
- Remote `origin` points to GitHub and tag `v0.1.0` exists.
- M01.12 is `Builder complete, awaiting QA`; it is not QA passed, completed, or merged.
- M01.13 remains `Not started`.
- M02 through M21 remain `Not started`.
- Product implementation has not started; no runtime security control, MoneyEvent runtime, ledger runtime, settlement runtime, reconciliation runtime, incident runtime, invariant engine, causal graph runtime, replay runtime, agent runtime, repair planner runtime, human-review runtime, UI, external connector, database schema, API route, GitHub Action, CI workflow, or product behavior was added.

M01.12 builder validation passed on 2026-05-21 after documentation-only THREAT_MODEL.md work:

- `docs/THREAT_MODEL.md` was rewritten as the canonical CausalLedger threat model.
- README, docs index, domain model, reliability model, active plan, roadmap, milestone doc, registry, status docs, capability matrix, tech debt, risk register, validation script, and bootstrap tests were updated for M01.12.
- Initial `python scripts/validate-control-plane.py` failed because `docs/status/NEXT_RECOMMENDED_THREAD.md` recorded M01.11 completion only with the full submilestone name while validation expected the exact shorter phrase; the scoped next-thread wording was fixed and validation was rerun successfully.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 29 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- M01.12 is `Builder complete, awaiting QA`; it is not QA passed or completed.
- M01.13 remains `Not started`.
- Product implementation has not started; no runtime security control, MoneyEvent runtime, ledger runtime, settlement runtime, reconciliation runtime, incident runtime, invariant engine, causal graph runtime, replay runtime, agent runtime, repair planner runtime, human-review runtime, UI, external connector, database schema, API route, GitHub Action, CI workflow, or product behavior was added.

M01.12 QA passed on 2026-05-23 after reviewing documentation-only THREAT_MODEL.md work:

- QA confirmed `docs/THREAT_MODEL.md` is a coherent CausalLedger threat model covering status, purpose, thesis, scope, protected assets, trust boundaries, actors/adversaries, evidence threats, ledger and financial truth threats, settlement/reconciliation threats, incident/replay threats, repair/human review threats, agentic AI threats, prompt injection threats, tool/permission threats, model routing and cost threats, privacy, secrets, supply chain, abuse and out-of-scope domain threats, evaluation/ablation threats, operational/governance threats, mitigation matrix, future dependencies, remaining M01 work, and guardrails.
- QA found no THREAT_MODEL.md content defects and no product implementation.
- QA changes were limited to tracking/status and control-plane validation expectation updates.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 29 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- M01.12 is `QA passed, awaiting merge`; it is not `Completed and merged` until PR merge and post-merge finalization.
- M01.13 remains `Not started`.
- Product implementation has not started; no runtime security control, MoneyEvent runtime, ledger runtime, settlement runtime, reconciliation runtime, incident runtime, invariant engine, causal graph runtime, replay runtime, agent runtime, repair planner runtime, human-review runtime, UI, external connector, database schema, API route, GitHub Action, CI workflow, auth/authz implementation, RBAC, encryption, secret management, audit-log implementation, or product behavior was added.

M01.12 post-merge finalization was recorded on 2026-05-23 after PR #31 merged and recent history showed duplicate PR merges #32 and #33 from the same branch:

- M01.12 is `Completed and merged`.
- Duplicate PR merges #32 and #33 are recorded as a process deviation.
- No revert is being done in this finalization thread.
- The corrective action is to restore tracking consistency, continue with stricter PR discipline, create only one PR per branch for future submilestones, and delete the branch after merge.
- Initial `python scripts/validate-control-plane.py` failed because the next-thread precondition used equivalent but non-exact wording; the scoped wording was fixed and validation was rerun successfully.
- `python scripts/validate-control-plane.py` passed.
- `python -m pytest tests/test_control_plane_bootstrap.py` passed with 29 tests.
- `git diff --check` passed.
- `make bootstrap-check` could not run because `make` is unavailable in the current Windows shell.
- M01.13 remains `Not started`.
- M02 through M21 remain `Not started`.
- Product implementation has not started.
