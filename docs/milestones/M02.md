# M02 Monorepo and local development

## Milestone goal

Create the runnable local development foundation.

## Why it matters

A predictable local environment is required before product packages can be safely built.

## Planning alignment

M02 planning PR #37 merged into `main` at commit `18148f7` (`M02 planning monorepo and local development environment (#37)`) on 2026-06-08.

M02.01 is `Completed and merged` after PR #38 merged into `main` at commit `fb2b901`. M02.01 chose and documented the backend/frontend stack and local development direction without implementing product behavior.

M02.02 is `Builder complete, awaiting QA` on branch `m02-02-create-apps-api`. It creates a minimal non-domain `apps/api` foundation and required workspace manifests only.

The active plan remains `plans/active/CLP-0003-m02-monorepo-and-local-development-environment.md` until M02 closeout.

## Detailed submilestones

| ID | Name | Status | Notes |
| --- | --- | --- | --- |
| M02.01 | Choose backend and frontend stack | Completed and merged | Documentation-only stack decision in ADR-0005 and ADR-0006; PR #38 merged at commit `fb2b901`; no runtime scaffolding, dependency install, API, UI, database, or MoneyEvent behavior was created by M02.01. |
| M02.02 | Create apps/api | Builder complete, awaiting QA | Minimal TypeScript/Fastify app foundation, workspace manifests, lockfile, and non-domain bootstrap test; no CausalLedger domain routes, health checks, database, auth/authz, connector, MoneyEvent, ledger, incident, evidence, replay, graph, repair, or product behavior. |
| M02.03 | Create apps/web | Not started | Future slice; do not start without an active plan. |
| M02.04 | Create apps/worker | Not started | Future slice; do not start without an active plan. |
| M02.05 | Create apps/agent-runtime | Not started | Future slice; do not start without an active plan. |
| M02.06 | Create packages/core | Not started | Future slice; do not start without an active plan. |
| M02.07 | Create packages/events | Not started | Future slice; do not start without an active plan. |
| M02.08 | Create packages/ledger | Not started | Future slice; do not start without an active plan. |
| M02.09 | Create packages/invariants | Not started | Future slice; do not start without an active plan. |
| M02.10 | Create packages/incidents | Not started | Future slice; do not start without an active plan. |
| M02.11 | Create packages/graph | Not started | Future slice; do not start without an active plan. |
| M02.12 | Create packages/replay | Not started | Future slice; do not start without an active plan. |
| M02.13 | Create packages/repair | Not started | Future slice; do not start without an active plan. |
| M02.14 | Add Postgres | Not started | Future slice; do not start without an active plan. |
| M02.15 | Add Redis | Not started | Future slice; do not start without an active plan. |
| M02.16 | Add Docker Compose | Not started | Future slice; do not start without an active plan. |
| M02.17 | Add migrations | Not started | Future slice; do not start without an active plan. |
| M02.18 | Add health checks | Not started | Future slice; do not start without an active plan. |
| M02.19 | Add CI baseline | Not started | Future slice; do not start without an active plan. |
| M02.20 | QA dev environment | Not started | Future slice; do not start without an active plan. |

## Acceptance criteria

- Local checks run consistently.
- Safety boundaries remain consistent with `AGENTS.md` and active docs.
- Scope is limited to this milestone and its active plan.
- Validation evidence is recorded in the active plan and status docs.
- Handoff notes identify what changed, what did not change, and the next safe thread.

## Validation expectations

Run control-plane validation, bootstrap tests, diff checks, and available TypeScript/package checks selected by the active plan. Health checks and CI baseline remain deferred to M02.18 and M02.19.

## Status

M02.01 is completed and merged. M02.02 is builder complete, awaiting QA. M02.03 through M02.20 remain `Not started`. M03 through M21 remain `Not started`.

Product implementation has not started.

## Handoff notes

- M02 planning QA passed, and PR #37 merged into `main`.
- M02.01 QA passed, and PR #38 merged into `main` at commit `fb2b901`.
- M02.02 builder created a non-domain API scaffold only.
- Do not begin M02.03 until M02.02 QA passes, the M02.02 PR merges into `main`, and post-merge tracking is finalized.
- Treat submilestones here as planned scope, not implemented behavior.
