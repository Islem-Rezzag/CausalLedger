# M03 Canonical MoneyEvent engine

## Milestone goal

Define and implement the canonical event representation in a lean, evidence-grounded sequence after planning QA and PR merge.

## Why it matters

All future ledger, invariant, incident, graph, replay, repair, and benchmark work depends on explicit MoneyEvent identity, provenance, source references, idempotency, amount, currency, actor, timestamp, and uncertainty semantics.

## Planning alignment

M03 planning is active under `plans/active/CLP-0004-m03-canonical-moneyevent-engine.md`.

This planning work is documentation and control-plane work only. It does not implement MoneyEvent behavior, runtime schemas, database tables, API routes, product UI, storage behavior, parsers, validators, connectors, ledger behavior, invariant behavior, incident behavior, replay behavior, repair behavior, or agent runtime.

## Detailed submilestones

| ID | Name | Status | Notes |
| --- | --- | --- | --- |
| M03.01 | Canonical MoneyEvent concept and contract planning | Not started | Define conceptual fields, lifecycle meaning, provenance, idempotency, uncertainty, and non-goals. No TypeScript types, runtime schemas, parsers, validators, storage, routes, or UI. |
| M03.02 | MoneyEvent TypeScript types and schema boundary | Not started | Introduce TypeScript type and schema boundary work in `packages/events/` after planning QA. No database tables, API routes, ingestion, storage, ledger posting, repair behavior, or agent tools. |
| M03.03 | Evidence-to-MoneyEvent mapping fixtures and simulator planning | Not started | Plan and seed controlled mapping fixtures from provider-like events, settlement rows, bank lines, refunds, chargebacks, and synthetic simulator outputs. No real connectors, evidence storage, live ingestion, or external integrations. |
| M03.04 | MoneyEvent validation and normalization rules | Not started | Define deterministic validation and normalization rules for identity, idempotency, money, currency, timestamps, source references, provenance, duplicates, missing evidence, and conflicts. No LLM judgment as validation. |
| M03.05 | MoneyEvent test fixtures and benchmark seed cases | Not started | Create deterministic fixtures and early seed cases for later MoneyFlowBench reuse. No benchmark scoring, product readiness, incident detection, replay correctness, or repair safety claims. |
| M03.06 | MoneyEvent QA and closeout | Not started | Run QA, verify forbidden scope, confirm deterministic tests and docs are aligned, and prepare M03 closeout. Do not mark M03 complete without QA PASS, merge confirmation, and closeout. |

## Acceptance criteria

- M03 planning creates and maintains exactly one active M03 plan.
- M03.01 through M03.06 remain `Not started` until the planning PR passes QA and merges.
- MoneyEvent behavior is implemented only in later scoped submilestones, with deterministic validation before claims are made.
- Raw evidence references, provenance, idempotency, amount, currency, actor, timestamp, source identity, and uncertainty considerations are preserved in scope.
- No database schema, API route, product UI, storage behavior, agent runtime, ledger posting, repair approval, raw evidence mutation, or money mutation is introduced by planning.
- Safety boundaries remain consistent with `AGENTS.md` and active docs.
- Validation evidence is recorded in the active plan and status docs.
- Handoff notes identify what changed, what did not change, and the next safe thread.

## Validation expectations

M03 planning uses control-plane validation, bootstrap tests, diff checks, package checks, QA development validation, and forbidden-scope inspection.

Future implementation submilestones must add deterministic tests for MoneyEvent types, schema boundaries, normalization, idempotency, provenance, fixtures, source mapping, duplicate handling, missing evidence, delayed evidence, and conflicting evidence before claiming behavior.

## Status

M03 planning QA has passed locally for PR #47 and is awaiting human merge. M03.01 through M03.06 are `Not started`.

Product/domain implementation has not started.

## Handoff notes

- Treat submilestones here as planned scope, not implemented behavior.
- M03 Planning QA passed for PR #47 as planning/control-plane QA only; no MoneyEvent behavior or product/domain implementation was added.
- The exact next action after QA PASS is human merge of PR #47 after remote checks are green and draft status is cleared if needed.
- The exact next thread after PR #47 merges is `M03.01 Builder - Canonical MoneyEvent concept and contract planning`.
- Do not start M03.01 until the planning PR merges.
