# Fixtures

`money-events/candidates.json` is the M03.05 controlled synthetic MoneyEvent candidate corpus. It is versioned deterministic test data for the existing source-neutral validator and normalizer.

The corpus contains no live payloads, credentials, production evidence, simulator output, benchmark results, or financial truth. Its normative boundary is documented in `docs/MONEYEVENT_FIXTURES_BENCHMARK_SEEDS.md`.
