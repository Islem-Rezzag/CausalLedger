# Evidence Package

`@causalledger/evidence` is the future evidence package boundary.

M02.05 creates only package scaffolding:

- package manifest;
- TypeScript config extending the root config;
- scaffold metadata export;
- bootstrap test;
- local build, typecheck, test, lint, and format-check scripts.

No CausalLedger product or domain behavior is implemented here. There are no financial schemas, ledger entries, balances, invariants, incident workflows, graph traversal, replay algorithms, repair logic, evidence storage, benchmark behavior, external connectors, agents, database behavior, raw evidence mutation, or money mutation.
